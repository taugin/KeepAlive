<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shrewd">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <instrumentation
        android:name="com.shrewd.cmp.DaemonInstrumentation"
        android:targetPackage="${applicationId}"
        android:targetProcesses="${applicationId},${applicationId}:resident,${applicationId}:daemon,${applicationId}:assist1,${applicationId}:assist2" />

    <application>
        <service
            android:name="com.shrewd.cmp.DaemonService"
            android:exported="false"
            android:process=":daemon" />

        <receiver
            android:name="com.shrewd.cmp.DaemonReceiver"
            android:exported="false" />

        <service
            android:name="com.shrewd.cmp.AService1"
            android:exported="false"
            android:process=":assist1" />

        <service
            android:name="com.shrewd.cmp.AService2"
            android:exported="false"
            android:process=":assist2" />

        <provider
            android:name=".cmp.DaemonProvider"
            android:authorities="${applicationId}.daemon"
            android:process=":daemon" />

        <provider
            android:name=".cmp.AssistantProvider"
            android:authorities="${applicationId}.assist1"
            android:process=":assist1" />

        <provider
            android:name=".cmp.PersistentProvider"
            android:authorities="${applicationId}.assist2"
            android:process=":assist2" />
    </application>

</manifest>